Open Xcode: 
cd ~/ && cd Desktop/git\ projects/Alien/ios && open Alien.xcworkspace
or
cd ~/ && cd Desktop/git\ projects/Alien/ios && open Alien.xcodeproj

---Pods uninstall
$: sudo gem install cocoapods-clean
$: pod deintegrate
$: pod clean
$: pod install
